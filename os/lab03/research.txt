1. Код возврата read равен 0, если произошла ошибка - 1, если произошло прерывание через n секунд через 128+n секунд
echo $? возвращает код ошибки,
2. echo -n позволяет выводить строку без перевода строки в конце.